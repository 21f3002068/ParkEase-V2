# Flask-VueJS App
A full-stack project with Flask (Python backend) and VueJS (frontend).

## Setup
1. **Backend**:  
   ```bash
   cd backend
   pip install -r requirements.txt 
   python app.py

2. **Frontend**:
    ```bash
    cd frontend
    npm install
    npm run serve